# SST Dashboard Environment Variables

# API URL.
# Leave blank when the API serves the built dashboard from the same host/port.
# Set this when hosting the web bundle separately, e.g. https://api.example.com.
VITE_SST_API_URL=
